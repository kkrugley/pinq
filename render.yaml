services:
  - type: web
    name: pinq-signaling
    env: node
    buildCommand: pnpm install && pnpm build --filter @pinq/signaling
    startCommand: node apps/signaling/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
